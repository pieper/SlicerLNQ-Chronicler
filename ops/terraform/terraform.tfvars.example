# Copy to terraform.tfvars (gitignored) for local runs.
# In CI, these come from GitHub secrets via workflow env vars.
#
# Authentication: set OS_CLOUD=<your-cloud-alias-in-clouds.yaml> in your
# shell before running tofu locally. The provider reads it directly.

domain_name             = "lnq-chronicle.isomics.dev"
floating_ip_address     = "149.165.152.66"
letsencrypt_email       = "you@example.com"
couchdb_admin_password  = "set-via-secret-not-here"
ssh_public_key          = "ssh-ed25519 AAAA... lnq-chronicler-deploy"

# Optional overrides:
# core_image       = "Featured-Ubuntu24"
# doorman_image    = "Featured-Ubuntu24"
# core_flavor      = "m3.medium"
# doorman_flavor   = "m3.tiny"
